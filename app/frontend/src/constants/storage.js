/** Cle localStorage : derniere configuration de partie utilisee par l'utilisateur. */
export const LAST_SETUP_KEY = 'bataille-navale:last-setup'

/** Prefixe localStorage : presets de camera par "scope" (gameId + joueur). */
export const CAMERA_PRESET_STORAGE_PREFIX = 'bataille-navale:camera-preset:'
